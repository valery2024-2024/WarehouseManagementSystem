# Changelog

Усі важливі зміни цього проєкту будуть описані в цьому файлі.

Формат базується на принципах Keep a Changelog.

# [v1.0.0] - 2026

## Додано

### Lab 35 — Business Logic та Persistence

* реалізовано JSON persistence layer;
* додано Product repository;
* додано Order repository;
* реалізовано DTO layer;
* реалізовано LINQ analytics;
* додано Strategy Pattern;
* реалізовано business policies;
* додано order processing engine;
* створено сервісний шар;
* покращено структуру проєкту.

### Lab 36 — Testing та Refactoring

* додано unit тести;
* додано integration тести;
* додано exception тести;
* реалізовано test strategy;
* створено test matrix;
* покращено покриття коду тестами;
* виконано refactoring repository layer;
* додано CI workflow;
* створено iteration-3 документацію.

### Документація

* README;
* class diagram;
* sequence diagram;
* release plan;
* changelog;
* vision document;
* iteration reports.

## Змінено

* покращено структуру persistence layer;
* покращено стабільність тестів;
* оптимізовано repository layer;
* оновлено структуру документації.

## Виправлено

* виправлено проблеми з JSON persistence;
* виправлено помилки integration tests;
* виправлено помилки exception handling;
* виправлено проблеми з repository refactoring.

## Покриття тестами

* unit tests;
* integration tests;
* exception tests;
* repository tests;
* service tests.

## CI/CD

* налаштовано GitHub Actions;
* автоматичний запуск тестів при push та pull request.
